# ============================================================================
# 小凯记账 · 依赖版本目录（Version Catalog）
#
# 所有依赖版本在此统一声明，模块内只允许使用 libs.xxx 引用，禁止硬编码版本号。
# 版本配对说明：
#   - AGP 8.7.3 要求 Gradle >= 8.9，JDK 17
#   - Kotlin 2.0.x 起 Compose Compiler 已并入 Kotlin 插件，无需单独指定 compiler 版本
#   - KSP 自 2.3.0 起改为独立版本号（不再带 Kotlin 前缀），最新稳定版需与 Kotlin 大版本匹配
#   - Kotlin 2.4.x 官方兼容区间：Gradle 7.6.3–9.5.0、AGP 8.5.2–9.1.0（本工程两者均落在区间内）
#   - Compose BOM 需与 Kotlin 2.4 的 Compose Compiler 匹配，过低会导致 compiler/runtime 版本校验失败
# ============================================================================

[versions]
agp = "9.4.0"
kotlin = "2.4.20"
ksp = "2.3.12"
composeBom = "2026.08.00"

hilt = "2.60.1"
hiltNavigationCompose = "1.2.0"

room = "2.8.5"
datastore = "1.1.1"
workManager = "2.10.0"

navigationCompose = "2.10.1"
lifecycle = "2.11.0"
activityCompose = "1.13.0"
coreKtx = "1.15.0"

coroutines = "1.11.0"
javax-inject = "1"
junit = "4.13.2"
truth = "1.4.4"
turbine = "1.2.0"

# 图表：Vico 3.x。注意 groupId 是 patrykandpatrick（单 r），
# 另一个 com.patrykandpatryk.vico（双 r）已停在 2.2.0，不要用。
vico = "3.3.1"

# 图片加载：Coil 3.x。groupId 已从 io.coil-kt 改为 io.coil-kt.coil3
coil = "3.6.2"

[libraries]
# ---- AndroidX 基础 ----
androidx-core-ktx = { module = "androidx.core:core-ktx", version.ref = "coreKtx" }
androidx-activity-compose = { module = "androidx.activity:activity-compose", version.ref = "activityCompose" }
androidx-lifecycle-runtime-ktx = { module = "androidx.lifecycle:lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { module = "androidx.lifecycle:lifecycle-viewmodel-compose", version.ref = "lifecycle" }
androidx-lifecycle-runtime-compose = { module = "androidx.lifecycle:lifecycle-runtime-compose", version.ref = "lifecycle" }
androidx-navigation-compose = { module = "androidx.navigation:navigation-compose", version.ref = "navigationCompose" }
androidx-work-runtime-ktx = { module = "androidx.work:work-runtime-ktx", version.ref = "workManager" }

# ---- Compose（BOM 统一版本，子库不写版本号）----
androidx-compose-bom = { module = "androidx.compose:compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { module = "androidx.compose.ui:ui" }
androidx-compose-ui-graphics = { module = "androidx.compose.ui:ui-graphics" }
androidx-compose-ui-tooling = { module = "androidx.compose.ui:ui-tooling" }
androidx-compose-ui-tooling-preview = { module = "androidx.compose.ui:ui-tooling-preview" }
androidx-compose-material3 = { module = "androidx.compose.material3:material3" }

# ---- 依赖注入 ----
hilt-android = { module = "com.google.dagger:hilt-android", version.ref = "hilt" }
hilt-compiler = { module = "com.google.dagger:hilt-android-compiler", version.ref = "hilt" }
androidx-hilt-navigation-compose = { module = "androidx.hilt:hilt-navigation-compose", version.ref = "hiltNavigationCompose" }

# ---- 本地存储 ----
androidx-room-runtime = { module = "androidx.room:room-runtime", version.ref = "room" }
androidx-room-ktx = { module = "androidx.room:room-ktx", version.ref = "room" }
androidx-room-compiler = { module = "androidx.room:room-compiler", version.ref = "room" }
androidx-datastore-preferences = { module = "androidx.datastore:datastore-preferences", version.ref = "datastore" }

# ---- 异步 ----
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "coroutines" }
kotlinx-coroutines-android = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-android", version.ref = "coroutines" }
kotlinx-coroutines-test = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "coroutines" }

# ---- 依赖注入标准注解（domain 等纯 JVM 模块不引入 Hilt）----
javax-inject = { module = "javax.inject:javax.inject", version.ref = "javax-inject" }

# ---- 图表（Vico 3.x）----
vico-compose = { module = "com.patrykandpatrick.vico:compose", version.ref = "vico" }

# ---- 图片加载（Coil 3.x）----
coil-compose = { module = "io.coil-kt.coil3:coil-compose", version.ref = "coil" }

# ---- 测试 ----
junit = { module = "junit:junit", version.ref = "junit" }
truth = { module = "com.google.truth:truth", version.ref = "truth" }
turbine = { module = "app.cash.turbine:turbine", version.ref = "turbine" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
android-library = { id = "com.android.library", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
hilt = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
